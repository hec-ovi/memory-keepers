[project]
name = "mk-agents"
version = "0.1.0"
description = "ADK agents: the monument, one agent per keeper, dream agents"
requires-python = ">=3.12"
dependencies = ["google-adk>=2.6", "mk-library", "mk-models"]

[tool.uv.sources]
mk-library = { workspace = true }
mk-models = { workspace = true }

[build-system]
requires = ["uv_build>=0.11"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "mk_agents"
