[project]
name = "memory-keepers"
version = "0.1.0"
description = "Workspace root; each box is a member package"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["library", "models", "agents", "engine", "dreaming", "voice"]

[dependency-groups]
dev = ["pytest>=8", "pytest-asyncio>=1", "httpx>=0.28"]
